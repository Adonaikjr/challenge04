import styled from "styled-components";

export const Container = styled.div`
   width: 100%;
   border: none;
   border-radius: 10px;
   background-color: ${porps => porps.theme.Alabaster};
   padding: 1rem;
   display: flex;
   flex-wrap: wrap;
   align-items: center;
   justify-content: space-evenly;
   
`